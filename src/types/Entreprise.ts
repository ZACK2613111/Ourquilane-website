export default interface EnterpriseTranslations {
    titleButton: string
    title: string
    description: string
    Button: string
  }
  
  export default interface LanguageContextType {
    translations: {
      entreprise: EnterpriseTranslations
    }
  }